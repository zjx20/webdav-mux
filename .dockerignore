# 白名单：构建只需要 go.mod、go.sum 和 Go 源码，其余文件不进入构建上下文。这样本地的
# config.yaml、证书私钥等文件（包括放在 internal/ 子目录里的）不会被发送给构建器，改文档也不会让构建缓存失效。
#
# 写法上刻意不排除任何子目录，只排除文件：部分构建工具（如 buildah/podman）遇到被排除的目录会整个跳过，
# 不再检查里面有没有 "!" 放行的文件。因此 internal/ 下是“排除所有带扩展名的文件，再放行 .go 文件”，
# 没有扩展名的文件仍会进入构建上下文。
# 新增顶层源码目录或非 .go 的构建输入（例如 go:embed 引用的文件）时，需要按同样的方式在这里放行。
*
!go.mod
!go.sum
!*.go
!internal
internal/**/*.*
!internal/**/*.go
